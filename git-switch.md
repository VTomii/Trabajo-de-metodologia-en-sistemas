# Git switch
Concepto: El comando `git switch` se utiliza para cambiar de una rama a otra. Está relacionado con [Git Checkout](git-checkout.md) ya que 
ambos comandos comparten la misma función.

## Sintaxis: 
git switch `nombre-de-la-rama` (para moverse a una rama existente)

